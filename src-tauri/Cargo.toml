[package]
name = "calculator"
version = "0.1.0"
description = "Вычислительный калькулятор"
authors = ["Your Name"]
edition = "2021"

[[bin]]
name = "calculator"
path = "src/main.rs"

[dependencies]
tauri = { version = "2.0.0", features = [] }  # ← Без features или с конкретными фичами
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"

[build-dependencies]
tauri-build = { version = "2.0.0", features = [] }
